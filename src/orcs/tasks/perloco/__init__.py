"""PerLoco — perceptive locomotion over (terrain, motion) pairs.

Registration lands in P3; today this package holds the staging interface and
its source plugins, which are pure readers (no sim, no torch, no mjlab).
"""
